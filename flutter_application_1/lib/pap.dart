import 'package:flutter/material.dart';


class Pap extends StatelessWidget {
  const Pap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pap'),
        backgroundColor: Color.fromARGB(255, 55, 148, 230),
      ),
    );
  }
}